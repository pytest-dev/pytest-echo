[tox]
envlist=py27,py33

[testenv]
deps = pytest
commands =
    py.test --junitxml={envlogdir}/junit-{envname}.xml {posargs:test_echo.py}

[pytest]
pep8ignore = E128 E302
    doc/conf.py ALL
addopts =
        -vvv
        --tb=short
        --capture=no

